import { Application, Request, Response } from 'express'

export const authEndpoint = (app: Application) => {
    app.post('/get-verify-token', async (request: Request, response: Response) => 
    {
        //
    }
    )
} 